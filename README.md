# Loopera

Loopera is a macOS Swift app for live video-loop performance with audio-first looping, camera preview, loop layouts, and session recording.

## Build

```sh
swift build
```

## Run as an app bundle

```sh
./script/build_and_run.sh
```

The app requests camera, microphone, and screen recording access for live video/audio capture and performance recording.
